import React from 'react'

const hero = props => {

    let content = (
        <header>
            <div class="name-box">
                <h1>MIKE ARCHER</h1>
            </div>
            <p>DEVELOPER || PROBLEM-SOLVER || CREATIVE</p>
        </header>
    )

    return content
}

export default hero